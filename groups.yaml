  #####################################################################
  ### Pool
  #####################################################################
  Pool:
    entities:
      - switch.zwave.pool_lights
      - input_select.pool_scene
      - input_select.pool_color